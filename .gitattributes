# Normalize all text files to LF
* text=auto eol=lf

# Binary files - no line ending conversion
*.pkl binary
*.gz binary
*.wav binary
*.npy binary
*.npz binary

# Git LFS
12_Angry_Men.wav filter=lfs diff=lfs merge=lfs -text
models/** filter=lfs diff=lfs merge=lfs -text